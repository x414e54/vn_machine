You’re building a virtual console with:

State RAM

Asset RAM

scratchpads

DMA engines

BUS arbitration

FPGA pipelines

NPU tiles

GPU tile engines

a supervisor CPU

deterministic tick loop

rewind/save/load

hardware‑shaped concurrency


VHDL/RTL to WASM.